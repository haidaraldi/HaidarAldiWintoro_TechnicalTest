@model List<BagianB_AplikasiWeb.Models.DataPelanggan>

@{
    ViewData["Title"] = "Data Pelanggan";
}

<h1>Data Pelanggan</h1>

<p>
    <a asp-action="Create">Tambah Baru</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                No
            </th>
            <th>
                Nama
            </th>
            <th>
                Alamat
            </th>
            <th>
                No. Telp
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>

        @for (int i = 0; i < Model.Count; i++)
        {
            <tr>
                <td>@(i + 1).</td>
                <td>@Model[i].Nama </td>
                <td>@Model[i].Alamat </td>
                <td>@Model[i].NoTelp </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@Model[i].Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@Model[i].Id">Detail</a> |
                    <a asp-action="Delete" asp-route-id="@Model[i].Id">Hapus</a>
                </td>
            </tr>
        }
    </tbody>
</table>
